# Create OAuth Client - Form Instructions

You're on the OAuth client creation form! Here's what to fill out:

## Fill Out the Form

### 1. Application Type
- Look for a dropdown/select field labeled **"Application type"**
- Select: **"Desktop app"**
  - NOT "Web application"
  - NOT "iOS"
  - NOT "Android"
  - **"Desktop app"** ✅

### 2. Name
- Look for a text field labeled **"Name"**
- Enter: **"Amprenta Gmail Client"**
- (Or any descriptive name you prefer)

### 3. Click CREATE
- Look for a blue button that says **"CREATE"**
- Click it

## What Happens Next

After clicking CREATE:

1. A popup window will appear
2. It will show:
   - Your Client ID (long string)
   - Your Client secret (long string)
   - A download icon (📥)
3. **Click the download icon (📥)** to download the JSON file
   - The file will download to your Downloads folder
   - File name will be something like: `client_secret_XXXXXX.json`

## If You See Other Fields

Some forms might have additional fields:
- **Authorized redirect URIs**: Leave empty (not needed for desktop app)
- **Authorized JavaScript origins**: Leave empty (not needed for desktop app)

Just focus on:
- Application type: **Desktop app**
- Name: **Amprenta Gmail Client**
- Click **CREATE**

## Tell Me

What fields do you see on the form? Can you see:
- Application type dropdown?
- Name field?
- Create button?

Fill them out and let me know when you've clicked CREATE!

