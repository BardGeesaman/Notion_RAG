# Optional remote state backend configuration
# To enable, uncomment and set your bucket/key/region, then run:
#   terraform init -backend-config="bucket=..." -backend-config="key=..." -backend-config="region=..."

# terraform {
#   backend "s3" {
#     bucket = "your-terraform-state-bucket"
#     key    = "terraform.tfstate"
#     region = "us-east-1"
#   }
# }

