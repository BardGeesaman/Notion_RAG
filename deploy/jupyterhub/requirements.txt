# Base notebook data stack
pandas
seaborn
matplotlib

# API client dependency
httpx

# Jupyter single-user server bits (needed by JupyterHub spawner)
jupyterlab
notebook

# RDKit is installed via conda in the Dockerfile (not pip)

# JupyterHub Docker-based spawner (production-style user isolation)
dockerspawner

# JWT validation for token-based auth
PyJWT>=2.8.0


