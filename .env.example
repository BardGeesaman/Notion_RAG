# Feature cache configuration
# Enable or disable dataset feature caching (default: true)
FEATURE_CACHE_ENABLED=true

# Time-to-live for cached dataset features in seconds (default: 3600)
FEATURE_CACHE_TTL=3600

# Maximum number of datasets to retain before LRU eviction (default: 1000)
FEATURE_CACHE_MAX_SIZE=1000
